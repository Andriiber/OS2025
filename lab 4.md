**ЗВІТ ПО ВИКОНАННЮ ЛАБОРАТОРНОЇ РОБОТИ №1**

з дисципліни: «Операційні системи»

**Тема: “Команди Linux для управління процесами”**

Виконали студенти 
групи КСМ-33 
Бердяєв А.О, Охріменко Є.Б  
Перевірила викладач  
Сушанова В.С. 

**Мета роботи:**  
Отримання практичних навиків роботи з командною оболонкою Bash.  
Знайомство з базовими командами для управління процесами.  




Завдання для попередньої підготовки:

*Прочитайте короткі теоретичні відомості до лабораторної роботи та зробіть невеличкий словник базових англійських термінів з питань призначення команд та їх параметрів.

На базі розглянутого матеріалу дайте відповіді на наступні питання:

2.1. *Які команди для моніторингу стану процесів ви знаєте. Як переглянути їх можливі параметри?

Основні команди для моніторингу процесів у Linux/Unix:

| Команда    | Призначення                                             
| ---------|-------------------------------------------------------
| ps| Виводить список поточних процесів                       
| ps axZ | Щоб отримати інформацію про безпеку:                        
| ps -U root -u root u | Щоб побачити кожен процес, що працює від імені root (real & effective ID), у форматі користувача  
| ps -ejh  | Відображає процеси у вигляді дерева                                     
| ps ax  | Щоб побачити кожен процес у системі за допомогою синтаксису BSD                

2.2. *Чи може команда ps у реальному часі відслідковувати стан процесів?

Ні, Команда ps показує знімок (snapshot) стану процесів на момент виконання команди.
Вона не оновлюється автоматично в реальному часі.

2.3. **За якими параметрами можливе сортування процесів в команді top? Як переключатись між ними?

У top можна сортувати процеси за різними параметрами:

| Ключ / Поле | Опис                                     |
| ----------- | ---------------------------------------- |
| P         | Сортування за використанням CPU (%)        |
| M         | Сортування за використанням пам’яті (%)    |
| T         | Сортування за часом роботи процесу (TIME+) |
| N         | Сортування за PID (ідентифікатор процесу)  |

Перемикатись можна задопомогою комбінації клавіш "Shift + P/M/T/N)

2.4. **Які команди для завершення роботи процесів ви знаєте?  

| Команда        | Опис                                                            | 
| -------------- | --------------------------------------------------------------- | 
| `kill`         | Надсилає сигнал процесу за PID                                  | 
| `killall`      | Завершує всі процеси із заданою назвою                          | 
| `pkill`        | Завершує процеси за іменем або шаблоном                         | 
| `xkill`        | У графічному середовищі завершує процес, на який клацнули мишею | 
| `top` / `htop` | Можна вибрати процес і натиснути `k` (kill)                     |







Прочитати матеріал про роботу з процесами у терміналі:

Процеси в Linux. Управління процесами 

Find out what processes are running in the background on Linux

Підготувати в електронному вигляді початковий варіант звіту:

Титульний аркуш, тема та мета роботи

Словник термінів

Відповіді на п.2.1-2.4 з завдань для попередньої підготовки

Хід роботи:
 Початкова робота в CLI-режимі в Linux ОС сімейства Linux:
 
Запустіть операційну систему Linux Ubuntu. Виконайте вхід в систему та запустіть термінал (якщо виконуєте ЛР у 401 ауд.).

Запустіть віртуальну машину Ubuntu_PC (якщо виконуєте завдання ЛР через академію netacad) 

Запустіть свою операційну систему сімейства Linux (якщо працюєте на власному ПК та її встановили) та запустіть термінал.

2. Дайте відповіді на наступні питання:

- Як вивести вміст директорії /proc? Де вона знаходиться та для чого призначена? Охарактеризуйте інформацію про її вміст?

      Де знаходиться:
      Директорія /proc — це віртуальна файлова система, розташована за шляхом /proc у кореневій файловій системі Linux.

Призначення:
```Вона містить інформацію про запущену систему та процеси. Дані не зберігаються на диску — це інтерфейс між ядром Linux і користувачем, що дозволяє "читати" поточний стан системи через файли.```

Як переглянути вміст:

    ls /proc

Типовий вміст:
    
    /proc/cpuinfo — інформація про процесор
    
    /proc/meminfo — використання пам’яті
    
    /proc/uptime — час роботи системи
    
    /proc/[PID]/ — каталог кожного процесу з ідентифікатором (PID)
    
    /proc/version — версія ядра Linux
    
    /proc/mounts — змонтовані файлові системи


- Як вивести інформацію про поточні сеанси користувачів. Якою командою це можна зробити?

Команди:

who або w

Пояснення:
    
    who показує, хто увійшов у систему, де і коли.
    
    w додатково показує, що саме користувачі роблять у системі.
    
    - *Чим відрізняється фоновий процес від звичайного. Де вони використовуються?
    
    - *Опишіть наступні команди та поясніть що вони виконують – команда jobs, bg, fg.
    
    - **Якою командою можна переглянути інформацію про запущені в системи фонові процеси та задачі?
    
    - **Як призупинити фоновий процес, як його потім відновити та при необхідності перезапусти?

- Які дії можна зробити в терміналі за допомогою комбінацій Ctrl + C, Ctrl + D та Ctrl + Z?

Комбінація Дія

    Ctrl + C Зупиняє (перериває) виконання поточного процесу (посилає сигнал SIGINT).
    Ctrl + D Завершує введення (EOF) або виходить із терміналу (якщо натиснуто в порожньому рядку).
    Ctrl + Z Призупиняє виконання процесу (посилає сигнал SIGTSTP) і переводить його у фоновий режим.


- *Чим відрізняється фоновий процес від звичайного. Де вони використовуються?

Тип процесу Характеристика

    Звичайний (foreground) Виконується у терміналі, блокує введення, поки не завершиться.
    Фоновий (background) Виконується у фоновому режимі, не блокує термінал, дозволяє вводити інші команди.


Запуск у фоновому режимі: bg

Використання:
Фонові процеси часто застосовують для тривалих або допоміжних завдань, наприклад, завантажень, серверів або обчислень.

  
- *Опишіть наступні команди та поясніть що вони виконують – команда jobs, bg, fg.

Команди:
    
    jobs - Показує список активних задач у поточному сеансі терміналу (фон/призупинені).
    bg - Переводить призупинену задачу у фоновий режим (продовжує виконання).
    fg - Повертає задачу у передній план (керування терміналом).


Приклад:

    sleep 100 &
    jobs          # побачите список задач
    fg %1         # повертає задачу з ID 1 у передній план
    bg %1         # відновлює задачу у фоні


- **Якою командою можна переглянути інформацію про запущені в системи фонові процеси та задачі?

У межах поточного сеансу:

jobs

У межах всієї системи:

    ps aux
    або
    top
    або
    htop


- **Як призупинити фоновий процес, як його потім відновити та при необхідності перезапусти?

Призупинити процес (у передньому плані):

    Ctrl + Z

Відновити у фоновому режимі:

    bg %номер_задачі

Відновити у передньому плані:

    fg %номер_задачі

Перезапустити (заново запустити процес):
    
    kill %номер_задачі   # завершити процес
    команда &            # запустити знову у фоні









3. Запустіть термінал, та в командному рядку виконайте наступні дії для ознайомлення з роботою з процесами:

запустіть команду top, проаналізуйте отриманий в цій команді результат та охарактеризуйте найбільш активні процеси у системі;
<img width="818" height="539" alt="image" src="https://github.com/user-attachments/assets/e91b0043-7419-4418-8bb6-7b4acba8e93e" />
Ctrl+Z - Зупиняє роботу команди top


призупинити виконання команди top (треба використати комбінацію клавіш);

вивести інформацію про процеси за допомогою команди ps;

*наведіть 5 прикладів з використанням різних параметрів команди ps (наприклад, вивести тільки системні процеси, вивести процеси конкретного користувача, вивести дерево процесів тощо). Опишіть, що саме роблять обрані Вами параметри

**передивіться чи є у Вас запущені фонові процеси, які саме?

**відновити виконання призупиненого фонового процесу спочатку у позиції “на передньому плані” (foreground), потім ще раз його призупинити, а потім відновити його виконання у позиції “на задньому плані” (background)
завершити роботу даного фонового процесу.

Контрольні запитання:

1. Яке призначення директорії /proc в системах Linux. Яку інформацію вона зберігає?

2. Як серед будь-яких трьох процесів динамічно визначати, який з них в поточний момент часу використовує найбільший обсяг пам'яті? Який відсоток пам’яті він споживає від загального обсягу?

3. Як отримати ієрархію батьківських процесів в системах Linux? Наведіть її структуру та охарактеризуйте.

4. *Чим відрізняється команда top від ps?

5. *Які додаткові можливості реалізує htop в порівнянні з top?

`htop` — це покращена версія утиліти `top`, що надає розширені можливості моніторингу процесів у Linux.

Переваги `htop` над `top`:

    Має інтерактивний графічний інтерфейс з кольоровим оформленням.
    Підтримує навігацію клавішами зі стрілками, що спрощує вибір і керування процесами.
    Дає змогу завершувати або змінювати пріоритет процесів (kill, renice)** без введення PID вручну.
    Відображає зручні графіки завантаження CPU, пам’яті, swap.
    Можна сортувати процеси за будь-яким полем (CPU, пам’ять, час тощо).
    Показує повну ієрархію процесів (tree view).

6. **Опишіть компоненти вашої мобільної ОС для здійснення моніторингу запущених в системі процесів?

Основні компоненти Android для моніторингу процесів:

    Activity Manager** — керує життєвим циклом застосунків та процесів, відстежує їх стан.
    Process Stats (у Developer Options)** — системний модуль, що відображає статистику споживання ресурсів процесами.
    ADB (Android Debug Bridge)** — інструмент для перегляду та керування процесами з комп’ютера через термінал (`adb shell top`, `ps`, `kill`).
    Settings > Apps > Running services** — базовий системний монітор запущених служб.

7. **Чи підтримує Ваша мобільна ОС термінальне керування роботою процесів, опишіть як саме.

Android підтримує термінальне керування процесами, хоча звичайним користувачам ці можливості обмежені.

Приклади:

    Через ADB shell можна використовувати стандартні команди Linux:
    ps — перегляд процесів;
    top або htop (якщо встановлено) — моніторинг у реальному часі;
    kill <PID> — завершення процесу;
    renice <PID> — зміна пріоритету процесу.
    На рутованих пристроях можливе повне керування системними процесами через термінал.


8. **Чи можливо поставити сторонні програмні засоби, що дозволяють організувати управління та моніторинг роботою процесів у Вашому мобільному телефоні. Коротко опишіть їх.

Так, існує багато сторонніх додатків для моніторингу процесів на Android:

Приклади:

    1. SystemPanel 2 — показує навантаження на процесор, пам’ять, мережу; дозволяє зупиняти процеси.
    2. Simple System Monitor — відображає активні процеси, графіки використання CPU, RAM, мережі.
    3. Termux — емулятор терміналу, у якому можна встановити htop, ps, kill тощо.
    4. 3C Toolbox — потужний інструмент для моніторингу системних ресурсів, керування службами, журналами.
















Оформлення звіту:

Титульний аркуш

Тема та мета роботи

Завдання попередньої підготовки

Основні позиції ходу роботи

Відповіді на контрольні запитання

Висновки за результатами роботи (обов’язково!!!)

Система оцінювання лабораторної роботи:

Виконано завдання базового рівня складності - 3 бали

Виконано завдання базового та середнього рівня складності - 4 бали 

Виконано завдання всіх рівнів складності (в тому числі й підвищеного) - 5 балів

Завдання середнього рівня складності позначені в завданнях (*)
Завдання підвищеного рівня складності позначені в завданнях (**)

Примітка: за виконання робіт в командах та оформлення звітів з використанням системи контролю версій (git) та англійської мови може бути нараховано додатковий 1 бал.
