**ЗВІТ ПО ВИКОНАННЮ ЛАБОРАТОРНОЇ РОБОТИ №3**

з дисципліни: «Операційні системи»

**Тема: «Знайомство з базовими командами CLI-режиму в Linux»**

Виконали студенти  
групи КСМ-33   
Бердяєв А.О,Охріменко Є.Б.   
Перевірила викладач  
Сушанова В.С. 

Київ 2025

**Мета роботи:**   
Знайомство з базовими командами CLI-режиму в Linux.  
Знайомство з базовими текстовими командами в термінальному режимі роботи в різних ОС.  

**Матеріальне забезпечення занять:**
1. ЕОМ типу IBM PC.
2. ОС сімейства Windows та віртуальна машина Virtual Box (Oracle).
3. ОС GNU/Linux (будь-який дистрибутив).
4. Сайт мережевої академії Cisco netacad.com та його онлайн курси по Linux

Завдання для попередньої підготовки:

1. *Прочитайте короткі теоретичні відомості до лабораторної роботи та зробіть невеликий словник базових англійських термінів з питань призначення команд та їх параметрів.

2. Вивчіть матеріали онлайн-курсу академії Cisco “NDG Linux Essentials”:

Chapter 5 - Command Line Skills

Chapter 6 - Getting Help

3. Пройдіть тестування у курсі NDG Linux Essentials за такими темами:

Chapter 05 Exam

Chapter 06 Exam


4. *Дайте визначення наступним поняттям:

(Охріменко Євген)

        Командний інтерпретатор – програма, що зчитує команди користувача, інтерпретує їх та передає на виконання (наприклад, bash, zsh, sh).
        
        Оболонка (shell) – інтерфейс між користувачем та операційною системою, що дозволяє працювати з командами, файлами, процесами.
        
        Команда – інструкція для оболонки, що виконує певну дію (наприклад, ls, cd, echo).


5. Дайте відповіді на наступні питання:

(Охріменко Єген)

- Яку базову інформацію надає рядок запрошення prompt?
    
    Рядок запрошення (prompt) — це текст у терміналі, який сигналізує користувачу про готовність оболонки приймати команди. Зазвичай він містить:
    
        * Ім’я користувача (user)
        * Ім’я комп’ютера або хосту (host)
        * Поточний каталог (current directory)
        * Символ, що позначає завершення (наприклад, $ для звичайного користувача, # для root)

- Для чого команді потрібні параметри та аргументи?
    
        * Аргументи — це об’єкти, над якими виконується команда (наприклад, імена файлів чи каталогів).
        * Параметри (опції) — це ключі або прапорці, які змінюють поведінку команди (наприклад, -l для детального виведення).

    Приклад:
    
        bash
        ls -l /home/user/Documents
    
    
    Тут /home/user/Documents — аргумент, -l — параметр.

- Яке призначення команд ls, які параметри та аргументи вона може мати? Наведіть 3 приклади.

    Команда ls використовується для відображення списку файлів і каталогів у поточному каталозі або вказаному шляху.
    
    Параметри та аргументи:
    
         -l — детальна інформація (права доступу, власник, розмір)
         -a — показати всі файли, включно з прихованими
         -h — виводити розмір файлів у зручному форматі (KB, MB)
    
    Приклади:
    
        bash
        ls -l
        ls -a /home/user
        ls -lh /var/log


- Яким чином можна використати історію команд, які переваги це надає?
    
    Команда history або клавіші ↑/↓ дозволяють переглядати попередні команди.
    Переваги:
        
        Швидке повторення або редагування команд
        Легке виправлення помилок
        Автоматизація повторюваних завдань
    
    Приклад:
    
        bash
        history
        !42   # виконує команду під номером 42



- Яке призначення команди echo?

Команда echo використовується для виведення тексту або значення змінних у термінал.

Приклад:

    bash
    echo "Hello, world!"
    echo $HOME


- Охарактеризуйте поняття змінної в оболонці Bash, які типи змінних вона підтримує? 

    Змінна — це іменований контейнер для збереження даних (текст, числа, шляхи).
    
    Типи змінних:
    
        Локальні змінні** — існують тільки в поточній сесії оболонки
        Глобальні (експортовані) змінні** — доступні для дочірніх процесів
        Спеціальні змінні** — наприклад, `$HOME`, `$PATH`, `$?`
    
    Приклад створення змінної:
    
        bash
        NAME="Ivan"
        echo $NAME

- Яке призначення команд env, export та unset?

            env — показує всі поточні змінні середовища
            export — робить локальну змінну глобальною, доступною для дочірніх процесів
            unset — видаляє змінну
    
    Приклад:
    
        bash
        MYVAR="test"
        export MYVAR
        unset MYVAR

- Які команди для отримання довідки по командам в терміналі ви знаєте?

        man <команда> — відображає повну документацію
        <команда> --help — коротка довідка
        info <команда> — розширена документація (не завжди доступна)
        whatis <команда> — короткий опис

Приклад:
    
    bash
    man ls
    ls --help
    


6. Підготувати в електронному вигляді початковий варіант звіту:

Титульний аркуш, тема та мета роботи

Словник термінів

Відповіді на п.4 та п.5 з завдань для попередньої підготовки


Хід роботи:

1. Опрацюйте всі приклади команд, що представлені у лабораторній роботі курсу NDG Linux Essentials - Lab 5: Command Line Skills та Lab 6: Getting Help. Створіть таблицю для опису цих команд 

(Андрій)

# Таблиця команд із NDG Linux Essentials — Lab 5 та Lab 6

| **Назва команди** | **Призначення та функціональність** |
|--------------------|--------------------------------------|
| `ls` | Виводить список файлів і каталогів у поточному каталозі. |
| `ls -l` | Відображає список файлів у довгому форматі з детальною інформацією (права доступу, власник, розмір, дата зміни). |
| `ls -l /tmp` | Виводить детальну інформацію про файли у каталозі `/tmp`. |
| `ls -l /home` | Виводить детальну інформацію про вміст каталогу `/home`. |
| `whoami` | Виводить ім’я поточного користувача. |
| `uname` | Виводить назву операційної системи (ядра). |
| `uname -n` | Виводить ім’я вузла (hostname) комп’ютера. |
| `uname --nodename` | Аналогічно до `uname -n` — показує ім’я вузла. |
| `pwd` | Виводить шлях до поточного робочого каталогу (Print Working Directory). |
| `echo Hi` | Виводить текст «Hi» у терміналі. |
| `history` | Виводить список раніше виконаних команд у поточній сесії. |
| `history 5` | Показує останні 5 виконаних команд. |
| `!9` | Повторно виконує команду з номером 9 із історії. |
| `echo Hello Student` | Виводить текст «Hello Student». |
| `echo $HISTSIZE` | Показує кількість команд, що зберігаються в історії (`HISTSIZE`). |
| `echo $PATH` | Виводить вміст змінної середовища PATH (шляхи, де шукаються команди). |
| `which date` | Показує повний шлях до виконуваного файлу команди `date`. |
| `type command` | Визначає тип команди (вбудована, зовнішня, alias). |
| `type cd` | Показує, що `cd` — це вбудована (builtin) команда Bash. |
| `which ls` | Показує шлях до зовнішньої команди `ls` (зазвичай `/bin/ls`). |
| `type cp` | Показує тип і розташування команди `cp`. |
| `type -a ls` | Відображає всі джерела команди `ls` (включно з alias). |
| `alias` | Виводить список усіх створених псевдонімів (alias). |
| `type vi` | Показує тип і шлях до програми `vi`. |
| `cd /bin` | Змінює поточний каталог на `/bin`. |
| `type vlc` | Перевіряє, чи встановлена програма `vlc` (якщо не знайдена — повідомлення про помилку). |
| `cd` | Повертає користувача до домашнього каталогу. |
| `echo Today is \`date\`` | Виконує команду `date` всередині `echo` і виводить поточну дату. |
| `echo Today is $(date)` | Альтернативний запис для командної підстановки. |
| `echo This is the command '\`date\`'` | Виводить текст із зворотними лапками без виконання команди. |
| `echo This is the command \\\`date\\\`` | Аналогічно — виводить текст із лапками, використовуючи escape-символ `\`. |
| `echo This is the command "\`date\`"` | Виконує `date`, навіть у подвійних лапках. |
| `echo D*` | Виконує розгортання шаблонів — виводить усі файли/каталоги, що починаються на D. |
| `echo "D*"` | Виводить буквально «D*» (без розгортання шаблону). |
| `echo Hello; echo Linux; echo Student` | Виконує кілька команд послідовно (через `;`). |
| `false; echo Not; echo Conditional` | Усі команди виконуються незалежно від результату попередніх. |
| `echo Start && echo Going && echo Gone` | Команди виконуються лише якщо попередня була успішною (`&&`). |
| `echo Success && false && echo Bye` | Виконання зупиняється після невдалої команди (`false`). |
| `date` | Виводить поточну дату і час. |
| `man <команда>` | Відкриває man page (manual page) для вказаної команди. |
| `man date` | Показує інструкцію для команди `date`. |
| `/текст` | У `man` або `less` — пошук тексту вперед. |
| `?текст` | У `man` або `less` — пошук тексту назад. |
| `n` | Перейти до наступного збігу під час пошуку. |
| `N` | Перейти до попереднього збігу під час пошуку. |
| `h` або `H` | Відкрити довідку (help) у переглядачі `less`. |
| `q` | Вийти з `man` або `less`. |
| `Space` / `f` / `PageDown` | Прокрутити на один екран вниз. |
| `b` / `PageUp` | Прокрутити на один екран вгору. |
| `Enter` / `↓` | Прокрутити на один рядок вниз. |
| `↑` | Прокрутити на один рядок вгору. |
| `man -k <ключове слово>` | Шукає усі `man`-сторінки, у яких у описі зустрічається ключове слово. |
| `apropos <ключове слово>` | Аналогічно `man -k`, виводить збіги за ключовим словом. |
| `man -f <назва>` | Показує, у яких секціях `man` є сторінки з цією назвою. |
| `man <номер_секції> <назва>` | Відкриває конкретну секцію `man`, наприклад `man 5 passwd`. |
| `whatis <назва>` | Короткий опис команди (аналог `man -f`). |
| `info <команда>` | Відкриває info page — детальнішу документацію, часто більш навчального характеру. |
| `<команда> --help` | Показує коротку довідку та приклади використання команди. |
| `ls /usr/share/doc` | Перегляд системного каталогу з документацією для встановлених пакетів. |
| `locate <назва>` | Шукає файли за назвою у базі файлів системи. |
| `locate -b "\\<назва>"` | Шукає файли з точною назвою (без часткових збігів). |
| `updatedb` | (для root) — оновлює базу даних, яку використовує `locate`. |
| `whereis <назва>` | Показує, де знаходиться виконуваний файл, його `man`-сторінки та інша документація. |

---

Робота в в терміналі (закріплення практичних навичок) обов'язково представити свої скріншоти:



<h3>2.1. Робота зі змінними (Variables) та псевдонімами (Aliases) в терміналі:</h3>

(Охріменко Євген)

Створіть змінні, що будуть містити Ваші імена та прізвища $var_name1, $var_name2, $var_name3

<p align="center"><img width="410" height="44" alt="image" src="https://github.com/user-attachments/assets/b7fd41eb-df5b-478e-bfd3-2f58744d103b" />


За допомогою команди echo виведіть імена студентів вашої команди


<p align="center"><img width="403" height="88" alt="image" src="https://github.com/user-attachments/assets/3547cc9e-6f9c-49fe-9548-91efae74fe94" />


Створіть псевдоніми mycal1, mycal2, mycal3 для команди cal для автоматичного виведення календарю вашого року народження


<p align="center"><img width="478" height="440" alt="image" src="https://github.com/user-attachments/assets/a04511b5-0bc5-4e05-86d2-98f7bfd58358" />


<h3>2.2. Робота з функціями (Functions) в терміналі:</h3>

Створіть функцію students_report, що порядково буде виводити спочатку імена студентів Вашої команди, а потім роки їх народження  


<p align="center"><img width="436" height="162" alt="image" src="https://github.com/user-attachments/assets/059c7c55-7a0c-4135-a4d3-2c594a72e596" />  

---

<p align="center"><img width="385" height="417" alt="image" src="https://github.com/user-attachments/assets/e0bbdbff-d8a7-47f1-b815-e6f7de58b280" />


<h3>2.3. Робота з лапками (Quoting) в терміналі. Виведіть в командному рядку наступні речення:</h3>

        “We create such variables as $var_name1, $var_name2, $var_name3, which stored our names Name1, Name2, Name3” (у реченні спочатку виводимо назви змінних, а потім їх вміст)


<p align="center"><img width="777" height="87" alt="image" src="https://github.com/user-attachments/assets/ee7305f4-3e75-4c8a-b349-9ec09005f28f" />


        “We create such Aliases as mycal1, mycal2, mycal3, which can show our calendars: Calendar1, Calendar2, Calendar3”  (у реченні спочатку виводимо назву команди-псевдонімів, потім вивід цих команд).

<p align="center"><img width="779" height="88" alt="image" src="https://github.com/user-attachments/assets/1e3cea9f-a5c7-4927-b55e-262ce4e4752e" />

<h3>2.4. Робота з інструкціями керування (Control Statements) в терміналі:.</h3>

Чи можна завдання 2.1 та 2.2 ходу роботи виконати через інструкції керування без написання окремої функції, як це буде виглядати?

Можна використати цикли for замість ф-ії students_report:

*Альтернатива 2.1:*

<p align="center"><img width="568" height="138" alt="image" src="https://github.com/user-attachments/assets/b7ca2362-5831-46ff-beb5-a8c891b5e695" />

*Альтернатива 2.2:*

<p align="center"><img width="568" height="158" alt="image" src="https://github.com/user-attachments/assets/c78217b2-32c9-4e9a-a91c-02825f10b678" />

<h3>2.5. Робота з командами довідки (Man Pages) в терміналі:</h3>

На прикладі команди uname продемонструйте як отримати довідку. На основі отриманої додаткової інформації наведіть 5 різних варіантів виводу результату інформації по даній команді з використанням 5 різних параметрів (Options)

*man uname:*

<p align="center"><img width="780" height="554" alt="image" src="https://github.com/user-attachments/assets/8ba75e60-bbeb-4677-b5f2-b5f4f2237ba3" />

*uname -s --- виведе назву операційної системи:*

<p align="center"><img width="325" height="42" alt="image" src="https://github.com/user-attachments/assets/35804a15-c863-4195-9a92-dbb45b5078a8" />

*uname -n --- Виведе ім'я хоста:*

<p align="center"><img width="315" height="42" alt="image" src="https://github.com/user-attachments/assets/20a1b983-b701-4b1e-ab6c-fb7174346cef" />

*uname -v --- Виведе додаткову інформацію про версію OC:*

<p align="center"><img width="477" height="44" alt="image" src="https://github.com/user-attachments/assets/4f4a6aa3-4e2c-41bb-873d-efd86721618a" />

*uname -r --- Виведе версію ядра:*

<p align="center"><img width="308" height="41" alt="image" src="https://github.com/user-attachments/assets/b6f44573-ffa3-4534-91f5-e4e22a021dc7" />

*uname -m --- Виведе апаратну архітектуру:*

<p align="center"><img width="363" height="45" alt="image" src="https://github.com/user-attachments/assets/b37b9107-5ec8-4345-bdd3-7ba2a1fe4695" />

*uname -a --- Вивести всю інформацію:*

<p align="center"><img width="771" height="66" alt="image" src="https://github.com/user-attachments/assets/c745ec86-16d6-4a60-be4a-99ac4d91183a" />


<h2>Контрольні запитання:</h2>

(Охріменко Євген) (1 - 4)

1. Які типи команд існують в оболонці Bash?

    В Bash команди можна розділити на кілька типів:
    
    1. Вбудовані команди (builtin) – команди, реалізовані всередині Bash, наприклад:
    
           cd, echo, pwd, export, history, alias.ss
    
    2. Зовнішні команди (external) – це програми чи скрипти, які знаходяться у файловій системі, наприклад:
    
           /bin/ls, /usr/bin/python, /usr/bin/grep.
    
    4. Функції Bash – користувацькі або системні функції, визначені всередині Bash.
       Приклад:
    
           bash
           myfunc() { echo "Hello"; }
           myfunc
       
    
    5. Аліаси (aliases) – скорочення для команд, наприклад:
    
           bash
           alias ll='ls -l'
  

2. Що таке змінні оточення? Які вони бувають. Як їх можна переглянути в терміналі?

    Змінні оточення (environment variables) – це змінні, які доступні всім процесам, запущеним від поточного сеансу Bash.
    
    Типи змінних оточення:
    
    1. Стандартні змінні – надають інформацію про середовище:
    
           HOME – домашній каталог користувача
           PATH – шляхи для пошуку виконуваних файлів
           USER – ім’я користувача
           SHELL – оболонка користувача
    
    2. Користувацькі змінні – створюються самим користувачем:
    
           bash
           export MY_VAR="Hello"
           
    
    Переглянути змінні оточення можна так:
    
            env – показує всі змінні оточення
            printenv – аналогічно
            set – показує всі змінні, включно з локальними


3. *Опишіть змінну $PS1. Як в терміналі переглянути її вміст?

    $PS1 – це змінна, яка відповідає за рядок запрошення у Bash. 
    
    Наприклад, стандартний вигляд:
    
          user@hostname:~$
      
    Переглянути її можна так:
    
          bash
          echo $PS1
  

4. *Як можна змінити значення змінної $PS1? Що при цьому відбудеться в рядку запрошенні в bash (рядок запрошення перед початком кожної команди). Як змінити значення цієї змінної не на поточний сеанс, а за замовчуванням?
    
    Змінити значення можна так:
    
        bash
        PS1=">>> "
    
    
    Після цього рядок запрошення зміниться на >>> .
    
    Щоб зміни були постійними, потрібно додати цю команду в один із файлів конфігурації Bash, наприклад:
    
        ~/.bashrc – для інтерактивних сеансів
        ~/.bash_profile – для сеансів логіну
    
    Наприклад:
    
        bash
        echo 'PS1=">>> "' >> ~/.bashrc
        source ~/.bashrc

(Андрій 5-8)

5. *Для чого використовують лапки в оболонці Bash? . 

У Bash є три види лапок:

Одинарні ' ' – зберігають текст без обробки (усе всередині сприймається буквально).

  bash
  
  echo 'Hello $USER'
  
 Виведеться: Hello $USER
  
Подвійні " " – дозволяють підстановку змінних і спеціальних символів, але захищають від інтерпретації більшості інших.

  bash
  
  echo "Hello $USER"
  
  Виведеться: Hello ім'я_користувача
  
Зворотні лапки  ` , або конструкція $(...) – використовуються для виконання команд і підстановки результату.

  bash
  
  echo "Today is $(date)"

6. **Для чого використовують інструкції керування, які їх види Ви знаєте?

Інструкції керування в Bash потрібні для управління логікою виконання сценаріїв.
Види:

Умовні оператори:

        if ... then ... elif ... else ... fi
        
        case ... esac
        
Цикли:

          for ... do ... done
          
          while ... do ... done
          
          until ... do ... done
          
Оператори переходу:

        break (вихід з циклу)
        
        continue (перехід до наступної ітерації)
        
        Функції (function name { ... }) – для структуризації коду.

7. **В чому різниця якщо в кінці рядку запрошення bash стоїть символ $ чи #?

$ – показує, що ви працюєте від **імені звичайного користувача.
        
        # – показує, що ви працюєте від **імені адміністратора (root).

8. **Яке призначення команд whereis та locate? Яка між ними відмінність?

whereis <ім’я_програми> – шукає виконуваний файл програми, її вихідні коди і man-сторінки.

locate <ім’я_файлу> – швидкий пошук файлів по всій файловій системі, використовуючи попередньо створену базу updatedb.

Відмінність:

        whereis шукає конкретні системні файли (бінарники, man-сторінки).
        
        locate шукає будь-які файли в системі за назвою, працює швидко, але іноді база може бути не оновлена.
        
**Conclusion**

During this laboratory work, we explored the fundamental concepts and commands of the Linux command-line interface (CLI). We learned how to use basic shell commands for navigation, file management, variable handling, and command execution. Additionally, we practiced working with aliases, functions, quoting mechanisms, and control statements, which are essential tools for writing efficient Bash scripts.

We also studied different methods of accessing system documentation — including the use of man, info, --help, and whatis — and understood how to find detailed information about any command. Through practical exercises, we gained hands-on experience in managing the environment, using variables, customizing the prompt, and applying control structures to automate tasks.

